{
  "name": "fuse-vaults",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ sharad-s/fuse-vaults",
    "create-local": "graph create --node http://localhost:8020/ sharad-s/fuse-vaults",
    "remove-local": "graph remove --node http://localhost:8020/ sharad-s/fuse-vaults",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 sharad-s/fuse-vaults"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
